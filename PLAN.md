# 진화하는 섬 — Open Design 제작 계획

작성일: 2026-09-11

## 요청 해석과 기준

제공된 `00-shared-design-principles.md`, `04-evolving-island.md`를 바탕으로 Open Design에서 한국어 교육용 탐구 웹앱을 제작한다. 원본 두 문서를 요구사항의 기준으로 유지한다.

## 제작 전 연결

- 확인: Open Design 0.22.2 설치, 서명된 실행 파일 확인.
- Codex의 open-design MCP 등록을 공식 패키지 명령으로 완료하고 enabled=true 및 stdio 연결을 확인했다.
- 현재 작업에서 연결을 불러올 수 없으면 재시작이 필요한 사실을 보고하며 생성 성공을 주장하지 않는다.
- 제작 모드는 사용자가 Local Codex + gpt-5.6-luna로 승인했다. Open Design 생성과 Luna 후속 수정으로 구현 및 로컬 검증을 완료했다.

## 학습 흐름

```mermaid
flowchart LR
  A[초기 유전자형 관찰] --> B[조건 설정과 예측]
  B --> C[한 세대 실행]
  C --> D[20세대와 10개 시드 비교]
  D --> E[선택과 중립 비교]
  E --> F[그래프 근거로 설명 수정]
```

## 구현 범위

- 한 종, 두 좌위 A/a·B/b, 이배체, 4개 표현형, 2개 환경.
- N=20/60/200, 기본 60. 실행 중 N 고정. 세대 0~20.
- 재조합률 0~0.5, 돌연변이 0. 초기 개체군을 복제하고 seed+i로 10회 반복.
- 부모의 상대 번식 가중치로 2N 배우자를 직접 생성하고 두 개씩 묶는다. 중간 배우자 풀 재추출 금지.
- 대립유전자 빈도는 2N, 표현형 빈도는 N을 분모로 사용한다.
- 평균·중앙값·10~90백분위와 각 시드 결과를 표시한다.
- 환경 전환은 10세대 이후 다음 자녀 생성부터 적용하고 화면에 적용 구간을 명시한다.
- 환경 계수는 교육용 가상 상수임을 표시한다. 실제 종 예측이나 검수된 실측치로 표현하지 않는다.

## 화면 방향

밝은 아이보리 바탕, 바다색과 숲색, 읽기 쉬운 한국어로 섬 탐구 화면과 유전 패널을 구성한다. 섬은 공간 맥락이며 표와 그래프가 실제 수치를 전달한다. 생성 이미지는 풍경 맥락에 사용하고 측정 근거로 사용하지 않는다.

데스크톱에서는 섬과 조작판을 나란히 배치하고, 모바일에서는 유전 표 → 조건 → 실행 → 결과를 우선한다. 현재 필수 버튼 하나에 gi-pulse, 모션 감소에서는 정적 강조를 적용한다. 작은 업데이트 내역 버튼과 실제 개발 날짜를 표시한다.

## 구현과 검증 책임

Astra는 기획·모델 리뷰·결과 리뷰를 담당하고 실제 구현·수정·릴리스는 Luna가 담당한다. 파일은 500줄 미만의 기능별 모듈로 분리한다.

검증은 시드 복제성, 개체 수 보존, 경계 빈도, 재조합, 중립 평균과 개체군별 분산, 선택 경향, 환경 전환, 입력 오류를 확인한다. 화면은 320/360/1280px, 키보드·초점·모션 감소·WebGL 대체를 확인한다. VoiceOver 구현과 검증은 제외한다.

로그인·영구 저장·점수·AI 채점·음성 기능을 추가하지 않는다. 배포와 HVC 등록·갤러리 동기화는 별도 요청 범위이다. 실제 생성 결과가 반환한 Studio/Preview 링크로 결과를 전달한다.

## 현재 상태

### 새 작업 연결 점검 (2026-09-11)

- 현재 작업에서 Open Design MCP 도구가 로드되었으며 `collect_brief`, `list_agents`, `start_run`, `get_run`이 사용 가능하다. 재등록은 하지 않았다.
- 설치 앱과 기존 등록의 `enabled=true`를 다시 확인했다.
- `start_run`의 모델 지정 항목과 실행 시작 기록에서 `gpt-5.6-luna` 지정을 확인했다.
- 사용자가 Local Codex + gpt-5.6-luna 실행을 승인했다. Brief는 기존 설계에 따라 웹앱·연결된 흐름·완성도 있는 UI로 확정했다. Open Design의 모델 목록에서 Luna 사용 가능과 Codex ChatGPT 로그인을 확인했다.
- Open Design에 '진화하는 섬' 프로젝트를 만들었다. 구현 실행은 Luna로 지정하고, 풍경 이미지 생성은 별도의 Luna 작업으로 진행한다.
- 생성 후 엔진 검증과 ego-browser 학생 흐름 검증을 구분하여 기록한다. 실행 모델이 불명확하면 Cloud/BYOK나 다른 모델로 전환하지 않는다.

### 구현 및 검증 완료

- Open Design 첫 실행은 생성 성공 상태로 종료했으나 내부 브라우저 연결 문제로 검증이 미완료였다.
- 같은 프로젝트를 Luna 작업으로 보완했다. 유전자형 9가지 집계, 선택/중립 비교, 독립 시드 스트림, 실제 Three.js 렌더링, 기록·초점·모바일·비교 분포를 수정했다.
- 엔진 테스트와 ego-browser 실제 학습 흐름 검증을 완료했다. WebGL 상실 및 2D 시작 환경에서도 실행과 비교를 확인했다.
- 320/360 CSS px에서 페이지 가로 넘침 없음. 1280px, 키보드, 모션 감소, 밝은 테마 유지 확인.
- 빈 시드 입력은 오류, 명시적 0은 정상 실행을 확인했다.
- 자세한 근거와 남은 검증 범위는 IMPLEMENTATION_REPORT.md 및 output/qa/browser-validation.json에 기록한다.

### 이전 작업 인계 기록

요구사항 확인과 제작 계획 작성 완료. Luna가 공식 설치 앱의 `--headless --mcp-install codex` 명령으로 MCP 등록을 완료했다. 이전 작업의 도구 목록에는 새 MCP가 반영되지 않아 이 작업으로 이어왔다. 앱 생성 및 검증은 미실시.

새 작업에서는 원본 두 문서와 이 계획을 읽고 Open Design 스킬의 Brief 수집 절차부터 시작한다. 먼저 사용자 지침의 Luna 구현 조건을 충족하는 실행 방식을 확인한다. 아직 Brief 수집, 프로젝트 생성, 생성 실행을 하지 않았으므로 재사용할 workflow/run ID는 없다.
