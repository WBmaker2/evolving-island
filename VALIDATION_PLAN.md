# 진화하는 섬 검증 계획

작성일: 2026-09-11. 실행 전 판정 기준이며 통과 기록이 아니다.

## 엔진 검토

1. 각 자녀 세대가 N개체·2N haplotype을 유지한다.
2. 같은 초기 상태와 같은 seed는 개체 배열까지 재현한다.
3. 대립유전자·haplotype 분모는 2N, 표현형·유전자형 분모는 N이다.
4. 부모 선택과 감수분열을 2N회 직접 수행하며 유한 배우자 풀을 다시 추출하지 않는다.
5. AB/ab 부모의 r=0에서는 AB/ab만, r=0.5에서는 네 조합의 표본 비율이 이론값에 근접한다.
6. 돌연변이가 없을 때 빈도 0과 1은 보존된다.
7. 반복마다 초기 상태를 복제하고 seed+i를 사용한다. 실행 조건과 기록은 이후 조작으로 바뀌지 않는다.
8. 환경 전환 조건은 자녀 11세대부터 새 가중치를 적용한다.
9. 중립 다중 시드 평균, 크기별 분산, 선택 방향은 통계적 기준으로 판정한다.
10. 허용 범위 밖 입력과 가중치 총합 0은 설명 가능한 오류이며 자동 보정하지 않는다.

## 실제 학생 흐름

초기 개체의 두 염색체 읽기 → 예측 기록 → 한 세대 → 20세대 → 10개 시드 → 선택/중립 비교 → 설명 수정과 세션 기록.

- 조건 변경 적용 시점과 초기화 의미가 명확한지 확인한다.
- 두 좌위 그래프, seed별 결과, 평균·중앙값·10~90백분위와 표의 일관성을 확인한다.
- 320/360/1280 CSS px에서 핵심 조작과 가로 넘침을 측정한다.
- 현재 필수 버튼 하나의 강조, 키보드·초점, Esc 선택 취소, 모션 감소를 확인한다.
- WebGL 대체 화면에서도 표·그래프·실행 흐름을 유지하는지 확인한다.
- 업데이트 내역, 풍경 이미지 캡션, 가상 계수·모형 한계를 확인한다.

VoiceOver 구현과 검증, 공개 배포, 교실 수용성 검증은 이번 확인 범위가 아니다.

## 증거 기록

Open Design 실행 완료 상태, 실제 모델 기록, 엔진 테스트 출력, ego-browser 조작 결과를 구분한다. Studio/Preview 링크는 해당 실행이 반환한 값을 사용한다. 링크는 현재 로컬 Open Design 실행에 의존하며 공개 배포 주소가 아니다.
